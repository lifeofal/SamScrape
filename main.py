from selenium import webdriver
from selenium.common.exceptions import NoSuchElementException
import chrome_Window_Options
from webdriver_manager.chrome import ChromeDriverManager
from chrome_Window_Options import chromeOptions
import page_Iteration

entry = dict.fromkeys(['ORIGIN', 'LATENT', 'GENRE', 'AGE'])
#Json get split by 3 now we need to combine



if __name__ == '__main__':
    #driver creation leads to the multi pool
    x = page_Iteration.PageIteration()






